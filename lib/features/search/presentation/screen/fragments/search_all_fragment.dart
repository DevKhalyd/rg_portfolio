import 'package:flutter/material.dart';

import '../../widgets/body/results_search.dart';

class SearchAllFragment extends StatelessWidget {
  const SearchAllFragment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const ResultsSearch();
  }
}
