import 'package:flutter/material.dart';


// TODO: End this methods
class MenuAboutMe extends StatelessWidget {
  const MenuAboutMe({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
      height: 300,
      width: 300,
    );
  }
}
