import 'package:flutter/material.dart';

class MenuOptions extends StatelessWidget {
  const MenuOptions({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: Add some validations of size...
    return Container(
      // TODO: Add the constrainst to the container
      color: Colors.red,
      height: 100,
      width: 300,
    );
    // TODO: Add my social media...
  }
}